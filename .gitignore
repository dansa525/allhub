# =====================================================
# 깃허브에 올라가면 안 되는 파일들
# =====================================================

# ✦ 가장 중요: API 키가 담긴 환경변수 파일 (절대 올리면 안 됨)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Node 패키지 (용량 크고 npm install로 복원 가능)
node_modules/

# 빌드 결과물 (Vercel이 자동 빌드함)
build/

# 시스템 파일
.DS_Store
Thumbs.db

# 에디터 설정
.vscode/
.idea/
.vercel
.env*
